
const config = {
    port:3000,
    host:"localhost",
    db:{
        host: "localhost",
        user: "root",
        password: "123",
        database: "node_db"
    }
    
}

module.exports = config;